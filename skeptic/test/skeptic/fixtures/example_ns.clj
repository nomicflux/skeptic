(ns skeptic.fixtures.example-ns
  "Loadable empty namespace used by core_test.clj to satisfy the
  preload-namespaces requirement when exercising check-project's
  orchestration via mocked file/* and check-namespace fns.")
